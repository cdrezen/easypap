Modifiez le fichier etape-4-mpi/evaluation/2048  pour y placer les paramètres optimaux (kernel, nombre de threads, variante, géométrie des tuiles) à utiliser pour la taille indiquées par le nom du fichier.
Il faut obligatoirement utiliser au moins 2 processus mpi qui participent efeectivement au travail.

Cas à considérer : 
  	       	./run -s 2048 -a spirals -n -mpi "  "

Enregistrez dans le répertoire etape-4-mpi votre rapport dans un fichier nommé rapport.pdf

Placez-vous dans le répertoire racine d'easypap et lancez le script etape-4/evaluation/sandCheck pour vérifier le bon fonctionnement.

N'oubliez pas de pousser vos modifications sur le dépôt dans la branche principale. 
